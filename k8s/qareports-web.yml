#
# Autoscale pods to have minumum of 2 pods up to 10 pods
# depending on CPU average load
#
apiVersion: autoscaling/v1
kind: HorizontalPodAutoscaler
metadata:
  name: qareports-web-autoscale
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: qareports-web-deployment
  minReplicas: 1
  maxReplicas: 10
  targetCPUUtilizationPercentage: 50

---

#
# Expose qareports-web pods externally
#
apiVersion: v1
kind: Service
metadata:
  name: qareports-web-service
spec:
  selector:
    app: qareports-web
  ports:
    - port: 80
  type: LoadBalancer
  externalIPs:
  - 192.168.50.10  # it'll take another dns address when in production

---

#
# Deploy initially one pod of qareports-web container
#
apiVersion: apps/v1
kind: Deployment
metadata:
  name: qareports-web-deployment
  labels:
    app: qareports-web
spec:
  replicas: 1
  selector:
    matchLabels:
      app: qareports-web
  template:
    metadata:
      labels:
        app: qareports-web
    spec:
      containers:
      - name: qareports-web
        image: qareports/squad:0.6
        command: ["sh", "-c"]
        args:
        - service apache2 start && squad --timeout=60 --workers=2 --fast --log-level DEBUG
        ports:
        - containerPort: 80
        envFrom:
        - secretRef:
            name: qareports-environment
        resources:
          requests:
            memory: "128M"
            cpu: "250m"
          limits:
            memory: "512M"
            cpu: "500m"
