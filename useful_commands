* update cluster
eksctl update cluster --name=<clusterName>

* see k8s dashboard
kubectl apply -f utils/kubernetes-dashboard.yml
kubectl proxy
sensible-browser http://localhost:8001/api/v1/namespaces/kubernetes-dashboard/services/https:kubernetes-dashboard:/proxy/

# get the doken by running
./bin/kubectl -n kubernetes-dashboard describe secret $(../bin/kubectl -n kubernetes-dashboard get secret | grep admin-user | awk '{print $1}')

# ssh into nodes
ssh -i secrets/chaws_ssh_key.pem ubuntu@3.208.231.75
 -> the pem file is generated by using a key-pair in aws
 -> i think we should use only one, and keep it in the 
 -> deploy repo and encrypt it with gpg
